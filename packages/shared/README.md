# Shared Package

Use this directory only for code consumed by multiple apps.

Rules:

- Keep single-app code inside that app.
- Require an owner before adding shared code.
- Require tests before shared code becomes permanent.
- Do not use this package as a dumping ground for helpers.
